# Specs históricas

Cada `PROMPT-*.md` acá es la spec original con la que se construyó una feature (patrón
spec + agente de este proyecto). Son contexto histórico de CÓMO y POR QUÉ se tomaron ciertas
decisiones — no son documentación de uso ni reflejan necesariamente el estado actual del
código, que siguió evolucionando después. Para el estado actual, ver
[docs/ARQUITECTURA.md](../ARQUITECTURA.md).
